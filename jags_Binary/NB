model{	

   for(i in 1 : N) {
      
      #### hazard
      ## control arm: trt[i]==0, treatment arm: trt[i]==1

      Y[i] ~ dbern(p[i])
      mu[i] <- exp(alpha1 + beta * trt[i])
      
      p[i] <- mu[i] / (1 + mu[i]) 
      }
      
      ### prior 
      
      alpha1 ~ dnorm(0,0.001)
      
      beta ~ dnorm(0,0.001)
      
      logOR_TC = beta
      
      
}